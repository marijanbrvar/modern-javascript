// for loops
let аrray = ['marijan', 'kira', 'mia', 'eva']

for (let index = 0; index < аrray.length; index++) {
    // const element = аrray[index];
    let html = `<div>${аrray[index]}</div>`
    
    console.log(html)
}
